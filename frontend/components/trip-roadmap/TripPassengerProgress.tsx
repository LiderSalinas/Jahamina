import type { ScenarioDefinition } from "./types";
export function TripPassengerProgress({ scenario }: { scenario:ScenarioDefinition }) { const occupied=Math.min(3,scenario.onboard+scenario.pending); return <section className="roadmap-card passenger-progress"><header className="roadmap-section-heading"><div><p className="roadmap-kicker">Comunidad a bordo</p><h2>Pasajeros</h2></div><b>{occupied}/4</b></header><div className="seat-progress" aria-label={`${occupied} de cuatro lugares ocupados`}>{[0,1,2,3].map(seat=><i className={seat>=occupied?"empty":""} key={seat}/>)}</div><div className="passenger-stats"><span><b>{scenario.onboard}</b> a bordo</span><span><b>{scenario.pending}</b> pendientes</span><span><b>{4-occupied}</b> disponibles</span></div><div className="passenger-faces"><span>RU</span><span>RM</span><span>MA</span><small>Paradas coordinadas, información privada protegida.</small></div></section>; }
