import type { RoadmapStop, ScenarioDefinition } from "./types";
export function TripNextStopCard({ stop,index,total,occupied,scenario }: { stop:RoadmapStop; index:number; total?:number; occupied?:number; scenario?:ScenarioDefinition }) { const stopTotal=total ?? 4; const passengerCount=occupied ?? scenario?.onboard ?? 0; return <section className="next-stop-card" aria-labelledby="next-stop-heading"><div className="next-stop-number"><small>Próxima parada</small><b>{index+1} de {stopTotal}</b></div><div><p className="roadmap-kicker">{stop.status === "current" ? "Actual" : "Pendiente"}</p><h2 id="next-stop-heading">{stop.passenger}</h2><p>{stop.detail}</p></div><dl><div><dt>Hora</dt><dd>{stop.time}</dd></div><div><dt>Ocupación</dt><dd>{passengerCount} pasajeros</dd></div></dl></section>; }
