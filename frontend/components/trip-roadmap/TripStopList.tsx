import type { RoadmapStop } from "./types";
export function TripStopList({ stops,selected,onSelect,showOccupancy=true }: { stops:RoadmapStop[]; selected:number; onSelect:(index:number)=>void; showOccupancy?:boolean }) { return <section className="roadmap-card"><header className="roadmap-section-heading"><div><p className="roadmap-kicker">Trayecto compartido</p><h2>Paradas</h2></div><span>{stops.length} puntos</span></header><ol className="stop-list">{stops.map((stop,index)=><li className={`${stop.status} ${selected===index?"selected":""}`} aria-current={stop.status==="current"?"step":undefined} key={`${stop.title}-${index}`}><button type="button" onClick={()=>onSelect(index)} aria-label={`Destacar parada ${index+1}: ${stop.title}`}><time>{stop.time}</time><span className="stop-order">{index+1}</span><span className="stop-copy"><b>{stop.title}</b><small>{stop.passenger} · {stop.detail}</small></span>{showOccupancy&&<em>{stop.occupancy}</em>}</button></li>)}</ol><p className="privacy-note">Mostramos zonas generales, nunca direcciones privadas completas.</p></section>; }
