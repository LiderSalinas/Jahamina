self.addEventListener("push",(event)=>{let data={};try{data=event.data?event.data.json():{};}catch{}const raw=typeof data.url==="string"?data.url:"/reservas";let url="/reservas";try{const parsed=new URL(raw,self.location.origin);if(parsed.origin===self.location.origin)url=parsed.pathname+parsed.search;}catch{}event.waitUntil(self.registration.showNotification(data.title||"Jahamina",{body:data.body||"Tenés una novedad en tu viaje.",icon:"/icons/jahamina.svg",badge:"/icons/jahamina.svg",tag:data.tag||"jahamina",data:{url}}));});
self.addEventListener("notificationclick",(event)=>{event.notification.close();const url=event.notification.data?.url||"/reservas";event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then((windows)=>{for(const client of windows){if("focus" in client){client.navigate(url);return client.focus();}}return clients.openWindow(url);}));});
